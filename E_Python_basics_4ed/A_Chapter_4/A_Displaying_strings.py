"""Review Exercises 4.1"""

# Print a string that uses double quotation marks inside the string.

print('"For those who never stop trying, the stars are just another destination, no a limit".MCH')

# Print a string that uses an apostrophe inside the string.

print("For those who never stop trying, the stars're just another destination, no a limit.MCH")

# Print a string that spans multiple lines, with whitespace preserved.

print('''For those who never stop trying, 
            the stars are just another destination, 
                no a limit.
                        MCH''')
# Print a string that is coded on multiple lines but displays on a siNgle line.

print('For those who never stop trying,\
the stars are just another destination,\
no a limit. MCH')